/*
export interface Sentence {
  test: (value: string) => number;
  sentence: string;
}

export class ColOne implements Sentence {
  getIndex(value: string): number {
    const mapper = {
      '1': new RegExp()
    }
  }
  sentence: string;

}

export const SENTENCES: Sentence[] = [
  {
    test: (value: string) =>
    items: string[];
  }
];
*/
